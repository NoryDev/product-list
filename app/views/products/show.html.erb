<p id="notice"><%= notice %></p>

<% @product.photos.each do |photo| %>
  <%= cl_image_tag photo.path, width: 400, height: 400, crop: :fill %>
<% end %>

<p>
  <strong>Name:</strong>
  <%= @product.name %>
</p>

<%= link_to 'Edit', edit_product_path(@product) %> |
<%= link_to 'Back', products_path %>
